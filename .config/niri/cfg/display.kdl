// ────────────── Output Configuration ──────────────
// You can run `niri msg outputs` to get the correct name for your displays.
// You will have to remove "/-" and edit it before it takes effect.
// https://github.com/YaLTeR/niri/wiki/Configuration:-Outputs

/- output "DP-1" {
    mode "2560x1440@359.979" // Set resolution and refresh rate
    scale 1 // No scaling (use 2 for HiDPI)
}

// El fix para la caída a 60Hz del panel interno tras un toggle de
// Ultimate/MUX ya no vive acá hardcodeado a un panel/resolución en
// particular -- ver ../../refresh-lock/ (corre solo via autostart.kdl,
// detecta el panel interno y su modo nativo por IPC en cualquier equipo).
